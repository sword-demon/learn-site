# 用不透明令牌加服务端吊销，不用 Session 或纯 JWT

学习端和管理端都是公共 SPA，既要无感续期，又要管理员能立即踢全部或某一个登录族。纯服务端 Session 被宪章排除；纯无状态 JWT 无法立刻作废已发出的访问凭证。因此登录凭证是不透明访问令牌加刷新令牌：哈希后写入 Redis，访问令牌 15 分钟、刷新令牌 7 天并轮换，每次受保护请求核验未吊销记录。学员令牌与后台令牌隔离。代价是 Redis 不可用时必须失败关闭，不能降级放行。
