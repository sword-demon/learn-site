{
  "name": "learn-site",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build:contracts": "pnpm -r --filter @learn-site/contracts run build",
    "build:admin": "pnpm --filter @learn-site/admin run build",
    "build:web": "pnpm --filter @learn-site/web run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}